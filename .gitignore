# Downloads — regenerated by pipeline 01–03
data/raw/
# Big intermediate rasters (regenerated); the committed product is site/tiles/
*.mbtiles
*.pmtiles
dem/*.tif
dem/*.tif.aux.xml
# Priority drop-ins are user data, not versioned
data/priority/*.tif
# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
# GEE / Earthdata creds — never commit
.netrc
*.private-key.json
credentials.json
# OS
.DS_Store
Thumbs.db

# Session scratch/test artifacts (mount won't let bash delete these; safe to remove on your machine)
_wtest.txt
_wtst2.txt
_unlinktest.tmp
_gittest/
